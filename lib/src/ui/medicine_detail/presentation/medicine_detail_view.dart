part of 'medicine_detail_page.dart';

class _MedicineDetailView extends StatelessWidget {
  const _MedicineDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
